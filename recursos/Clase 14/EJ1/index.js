var btn = document.getElementsByTagName('button');
var popupDiv = document.getElementsByClassName('popup_main_div');
//var closePopup = document.getElementsByClassName("close_popup");

span.replaceChild(newChild, oldChild);

  function openPopUp() {
    popupDiv[0].style.display="block"; 
  }
  /* Btn.addEventListener('click',function(){
    PopupDiv.style.display="block"
  }) */

  function closePopUp() {
    popupDiv[0].style.display="none";
  }

  /* ClosePopup.addEventListener('click',function(){
    PopupDiv.style.display="none"
  }) */
